# ---- Anthropic (Claude) ----
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxx

# ---- Meta WhatsApp Cloud API ----
# From your WhatsApp > API Setup page in Meta for Developers
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id_here
WHATSAPP_ACCESS_TOKEN=your_temporary_or_permanent_access_token_here
WHATSAPP_BUSINESS_ACCOUNT_ID=your_waba_id_here

# A secret string YOU make up. Meta asks for this when you set up the webhook
# in "Configuration" - it must match exactly what you type into Meta's dashboard.
WEBHOOK_VERIFY_TOKEN=aura-spa-verify-2026

# The real business owner's WhatsApp number (with country code, no + or spaces)
# This is where escalation alerts get sent when the AI can't handle something.
OWNER_WHATSAPP_NUMBER=256759857029

# Port for the server (Railway/Render set this automatically, safe to leave)
PORT=3000
